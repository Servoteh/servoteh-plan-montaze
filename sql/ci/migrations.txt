# =============================================================================
# Spisak migracija koje CI primenjuje na praznu Postgres bazu POSLE bootstrap-a.
# Redosled je bitan. Komentari (# ...) i prazni redovi se ignorišu.
#
# Namerno preskačemo:
#   - sql/migrations/add_loc_step4_pgcron.sql — zahteva `pg_cron` ekstenziju
#     koja nije dostupna van Supabase/self-hosted PG sa posebnom konfiguracijom.
#     Scheduler logika se smoke-testira na Supabase-u manuelno.
#   - sql/migrations/add_loc_module_step1_tables.sql — subset add_loc_module.sql;
#     idempotentne DDL naredbe bi prošle, ali je redundantno.
# =============================================================================

sql/migrations/add_loc_module.sql
sql/migrations/add_loc_step2_ci_unique.sql
sql/migrations/add_loc_step3_cleanup.sql
sql/migrations/add_loc_step5_sync_rpcs.sql

# Faza 2 security / lokacije — potrebno za pgTAP (has_edit_role, RLS, audit, HALA↔POLICA pravila)
sql/migrations/add_menadzment_full_edit_kadrovska.sql
sql/migrations/enable_user_roles_rls_proper.sql
sql/migrations/add_audit_log.sql
sql/migrations/add_audit_actor_attribution.sql
sql/migrations/add_loc_location_hierarchy_rules.sql
sql/migrations/add_loc_locations_audit.sql

# Održavanje (CMMS) — stek: modul, katalog mašina, odgovorni, lokacije, supertablica sredstava
# (bootstrap u `00_bootstrap.sql` daje `project_id` + stub `bigtehn_machines_cache` za CI)
sql/migrations/add_maintenance_module.sql
sql/migrations/add_maint_machines_catalog.sql
sql/migrations/add_maint_machine_responsible.sql
sql/migrations/add_maint_locations.sql
sql/migrations/add_maint_assets_supertable.sql
sql/migrations/add_maint_work_orders.sql
sql/migrations/link_maint_incidents_to_wo.sql
sql/migrations/extend_maint_incidents_assets.sql
sql/migrations/add_maint_documents.sql
sql/migrations/add_maint_inventory.sql
sql/migrations/add_maint_vehicle_details.sql
sql/migrations/add_maint_it_asset_details.sql
sql/migrations/add_maint_facility_details.sql
sql/migrations/add_maint_settings.sql
sql/migrations/integrate_maint_settings_behavior.sql
sql/migrations/add_maint_preventive_auto_wo.sql
sql/migrations/add_maint_daily_ops_view.sql
sql/migrations/extend_maint_cmms_daily_summary.sql

# Projektni biro — pb_tasks, pb_work_reports, load RPC
sql/migrations/add_pb_module.sql
sql/migrations/pb_load_stats_mechanical_engineering.sql
sql/migrations/add_pb_notifications.sql
sql/migrations/add_pb_constraints.sql

# Faza 1 security hardening (2026-04-23). Migracija je idempotentna i
# preživljava izvrsavanje cak i ako v_production_operations ne postoji
# (REVOKE FROM anon na nepostojecem objektu padne — pa je u CI svjesno
# preskocena dok ne ukljucimo full plan-proizvodnje migracije u CI listu).
# sql/migrations/revoke_anon_v_production_operations.sql
