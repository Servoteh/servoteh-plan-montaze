/**
 * Podešavanja → Matični podaci (placeholder).
 *
 * Kasnije: lokacije, odeljenja, timovi, kategorije projekata.
 */

export function renderMastersTab() {
  return `
    <main class="kadrovska-main">
      <div class="kadrovska-empty" style="margin-top:30px;">
        <div class="kadrovska-empty-title">Matični podaci — u izradi</div>
        <div>Lokacije, odeljenja, timovi, kategorije projekata. Biće dostupno u sledećoj fazi.</div>
      </div>
    </main>
  `;
}
